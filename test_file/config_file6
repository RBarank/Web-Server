team "Razzle-Dazzle";

server {
    listen   3000;

    path /echo {
          action echo;             
    }

    path /static {
          action static;
          root /static;
          add extra stuff, shouldn't crash;
    }
}

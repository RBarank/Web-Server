team "Razzle-Dazzle";

server {
    listen   3000;

    path /echo {
          action echo;             
    }

    path /static {
          action static;
          root /static;
    }

    path /static2 {
        action static;
        root /testing_folder;
    }

    path /static3 {
        action static;
        root /test;
    }
}
